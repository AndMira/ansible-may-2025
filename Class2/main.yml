- hosts: all
  become: yes
  tasks:
  - name: Install Apache
    include: apache.yml
  - name: Install packeges
    include: php.yml
  - name: Install wordpress
    include: wordpress.yml

  - name: Install datebase
    include: db.yml